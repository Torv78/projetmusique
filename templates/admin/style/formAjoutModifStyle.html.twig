{% extends 'base.html.twig' %}

{% block title %}Ajout/modif Style{% endblock %}

{% block body %}

<h1 class="pt-2 text-center">
{% if (app.request.attributes.get('_route')=="app_admin_style_ajout") %}
    ajout
    {% else %}
    modification
{% endif %}
 d'un style</h1> <!-- Correction de la balise -->


{{form_start(formStyle)}}

<div class="row">
    <div class="col">{{form_row(formStyle.nom)}}</div>
    <div class="col-5">{{form_row(formStyle.couleur)}}</div>
</div>





<input type="submit" , class="btn btn-outline-success" value=
{% if (app.request.attributes.get('_route')=="app_admin_style_ajout") %}
    valider
    {% else %}
    modifier
{% endif %}>
<a href="{{path("app_admin_style")}}" class="btn btn-outline-warning">annuler</a>
{{ form_end(formStyle)}}
{% endblock %}
